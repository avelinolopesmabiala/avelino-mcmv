<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-YM0GLCB84S"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-YM0GLCB84S');
  </script>

  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Minha Casa Minha Vida | Avelino Lopes</title>
  <meta name="description" content="Faça uma simulação gratuita do Minha Casa Minha Vida e descubra como conquistar seu primeiro imóvel com subsídio e FGTS." />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #0A4DA2;
      --secondary: #2FBF71;
      --bg: #F5F7FA;
      --text: #1F2937;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg);
      color: var(--text);
    }

    header { background: linear-gradient(135deg, var(--primary), #06326a); color: white; padding: 32px 20px 64px; }
    .container { max-width: 1100px; margin: auto; }
    .logo { display: flex; align-items: center; gap: 12px; font-weight: 700; font-size: 22px; }
    .logo svg { width: 42px; height: 42px; }
    .hero { margin-top: 40px; display: grid; grid-template-columns: 1fr 1fr; gap: 32px; align-items: center; }
    .hero h1 { font-size: 42px; line-height: 1.2; }
    .hero p { margin: 16px 0 24px; font-size: 18px; }
    .btn { background: var(--secondary); color: white; padding: 16px 28px; border-radius: 8px; border: none; font-size: 18px; cursor: pointer; font-weight: 600; }
    .benefits { background: white; padding: 48px 20px; }
    .benefits ul { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 20px; list-style: none; }
    .benefits li { background: var(--bg); padding: 20px; border-radius: 10px; font-weight: 600; }
    .testimonials { padding: 48px 20px; }
    .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 20px; }
    .card { background: white; padding: 24px; border-radius: 12px; box-shadow: 0 10px 20px rgba(0,0,0,.08); }
    footer { background: #0f172a; color: white; text-align: center; padding: 24px; font-size: 14px; }
    .modal { position: fixed; inset: 0; background: rgba(0,0,0,.6); display: none; align-items: center; justify-content: center; z-index: 1000; }
    .modal-content { background: white; width: 100%; max-width: 500px; padding: 24px; border-radius: 12px; max-height: 90vh; overflow-y: auto; }
    .modal-content h2 { margin-bottom: 16px; }
    input, select { width: 100%; padding: 12px; margin-bottom: 12px; border-radius: 6px; border: 1px solid #ccc; }
    .whatsapp { position: fixed; bottom: 20px; right: 20px; background: #25D366; color: white; width: 60px; height: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 28px; text-decoration: none; z-index: 999; }
    @media(max-width: 768px){ .hero { grid-template-columns: 1fr; } .hero h1 { font-size: 32px; } }
  </style>
</head>
<body>

<header>
  <div class="container">
    <div class="logo">
      <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M8 30L32 8L56 30V56H38V40H26V56H8V30Z" fill="#2FBF71"/>
      </svg>
      Avelino Lopes
    </div>

    <div class="hero">
      <div>
        <h1>Realize o sonho da casa própria com o Minha Casa Minha Vida</h1>
        <p>Use seu FGTS, aproveite subsídios do governo e parcelas que cabem no seu bolso.</p>
        <button class="btn" onclick="openModal()">Fazer Simulação</button>
      </div>
      <img src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c" alt="Família feliz" style="width:100%;border-radius:16px;">
    </div>
  </div>
</header>

<section class="benefits">
  <div class="container">
    <ul>
      <li>✔ Subsídio do Governo Federal</li>
      <li>✔ Use o FGTS na entrada</li>
      <li>✔ Juros mais baixos do mercado</li>
      <li>✔ Parcelas acessíveis</li>
    </ul>
  </div>
</section>

<section class="testimonials">
  <div class="container">
    <h2>Quem já realizou o sonho</h2>
    <div class="cards">
      <div class="card">"Achei que nunca conseguiria, mas hoje estou no meu apartamento."<br><strong>Maria S.</strong></div>
      <div class="card">"O processo foi rápido e sem burocracia."<br><strong>João P.</strong></div>
      <div class="card">"Parcela menor que o aluguel."<br><strong>Ana R.</strong></div>
    </div>
  </div>
</section>

<footer>
  © 2026 — Avelino Lopes • Especialista MCMV
</footer>

<div class="modal" id="modal">
  <div class="modal-content">
    <h2>Simulação Gratuita e Sem Compromisso</h2>
    <form id="formPrincipal" onsubmit="enviarWhatsApp(event)">
      <input name="nome" placeholder="Nome Completo" required>
      <input name="cpf" placeholder="CPF" required>
      <input name="rg" placeholder="RG" required>
      <input name="email" type="email" placeholder="E-mail" required>
      <input name="whatsapp" placeholder="WhatsApp com DDD" required>
      <select name="renda" required>
        <option value="">Renda Familiar Mensal</option>
        <option>Até R$ 2.640</option>
        <option>R$ 2.641 a R$ 4.400</option>
        <option>R$ 4.401 a R$ 8.000</option>
      </select>
      <select name="estadoCivil" id="estadoCivil" required>
        <option value="">Estado Civil</option>
        <option value="solteiro">Solteiro(a)</option>
        <option value="casado">Casado(a)</option>
      </select>
      <select name="tipoTrabalho" id="tipoTrabalho" required>
        <option value="">CLT ou Autônomo</option>
        <option value="CLT">CLT</option>
        <option value="Autonomo">Autônomo</option>
      </select>
      <div id="holerite" style="display:none">
        <small>📄 Se for CLT, será solicitado apenas o último holerite</small>
      </div>
      <select name="registro" required>
        <option value="">Possui 3 anos de registro?</option>
        <option>Sim</option>
        <option>Não</option>
      </select>
      <button class="btn" type="submit">Quero Simular</button>
    </form>

    <div id="conjuge" style="display:none;margin-top:20px;">
      <h3>Dados do Cônjuge</h3>
      <input placeholder="Nome Completo do Cônjuge">
      <input placeholder="Profissão do Cônjuge">
      <select><option>CLT</option><option>Autônomo</option></select>
    </div>
  </div>
</div>

<a class="whatsapp" href="https://wa.me/5511989769685?text=Ol%C3%A1,%20gostaria%20de%20fazer%20uma%20simula%C3%A7%C3%A3o%20do%20Minha%20Casa%20Minha%20Vida" target="_blank">💬</a>

<script>
function openModal(){ document.getElementById('modal').style.display = 'flex'; }
document.getElementById('modal').addEventListener('click', e => { if(e.target.id === 'modal') e.target.style.display = 'none'; });
document.getElementById('estadoCivil').addEventListener('change', function(){ document.getElementById('conjuge').style.display = this.value === 'casado' ? 'block' : 'none'; });
document.getElementById('tipoTrabalho').addEventListener('change', function(){ document.getElementById('holerite').style.display = this.value === 'CLT' ? 'block' : 'none'; });
function enviarWhatsApp(e){
  e.preventDefault();
  const form = e.target;
  const dados = new FormData(form);
  let mensagem = `Olá, fiz a simulação no site e gostaria de continuar.\n`;
  mensagem += `Nome: ${dados.get('nome')}\n`;
  mensagem += `CPF: ${dados.get('cpf')}\n`;
  mensagem += `RG: ${dados.get('rg')}\n`;
  mensagem += `WhatsApp: ${dados.get('whatsapp')}\n`;
  mensagem += `Renda: ${dados.get('renda')}\n`;
  mensagem += `Estado Civil: ${dados.get('estadoCivil')}\n`;
  mensagem += `Trabalho: ${dados.get('tipoTrabalho')}\n`;
  mensagem += `3 anos de registro: ${dados.get('registro')}\n`;
  const url = `https://wa.me/5511989769685?text=${encodeURIComponent(mensagem)}`;
  window.open(url, '_blank');
}
</script>

</body>
</html>
